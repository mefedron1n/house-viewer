(() => {
  const { request, user } = window.studioContext, main = document.querySelector("#studio-main");
  const $ = (selector, root = document) => root.querySelector(selector), $$ = (selector, root = document) => [...root.querySelectorAll(selector)];
  const esc = (value) => String(value ?? "").replace(/[&<>"']/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" })[char]);
  const view = new URLSearchParams(location.search).get("view") || "profile";
  $$('[data-studio-nav]').forEach((link) => link.classList.toggle("active", link.dataset.studioNav === view));
  $$('.studio-mobile-nav a').forEach((link) => link.classList.toggle("active", link.href === location.href || (view === "dashboard" && link.href.includes("view=projects"))));
  const projectCard = (project) => `<a class="project-row" href="./project.html?id=${encodeURIComponent(project.id)}"><img src="./images/editorial-house.webp" alt=""><div><h3>${esc(project.name)}</h3><p>${project.area} м² · ${project.rooms} комнат</p></div><small>${new Date(project.updatedAt).toLocaleDateString("ru")}</small><span class="status-dot">${esc(project.status)}</span><span class="row-arrow">→</span></a>`;
  function profile() {
    const parts = user.name.trim().split(/\s+/), firstName = parts[0] || "", lastName = parts.slice(1).join(" ");
    main.innerHTML = `<section class="account-content"><header class="account-heading"><h1>Профиль</h1><p>Управляйте своей личной информацией и настройками аккаунта.</p></header><section class="account-card"><header class="account-card-head"><h2>Личные данные</h2><p>Обновите информацию о себе.</p></header><form id="account-profile-form" class="profile-form-grid"><div class="account-avatar-wrap"><div class="account-avatar" aria-hidden="true">♟</div><button class="avatar-button" type="button" disabled>Изменить фото</button></div><label class="account-field">Имя<input name="firstName" value="${esc(firstName)}" maxlength="40" required></label><label class="account-field">Фамилия<input name="lastName" value="${esc(lastName)}" maxlength="40"></label><label class="account-field account-field-email">Email<input name="email" type="email" value="${esc(user.email)}" maxlength="254" required></label><div class="account-form-actions"><button class="account-primary" type="submit">Сохранить изменения</button></div></form><p class="account-status" id="profile-status" role="status"></p></section><section class="account-card" id="account-password"><header class="account-card-head"><h2>Смена пароля</h2><p>Используйте надёжный пароль для защиты аккаунта.</p></header><form id="account-password-form" class="password-form"><label for="current-password">Текущий пароль</label><input id="current-password" name="currentPassword" type="password" autocomplete="current-password" placeholder="Введите текущий пароль" required><label for="new-password">Новый пароль</label><input id="new-password" name="newPassword" type="password" autocomplete="new-password" minlength="8" maxlength="128" placeholder="Введите новый пароль" required><label for="confirm-password">Подтвердите новый пароль</label><input id="confirm-password" name="confirmPassword" type="password" autocomplete="new-password" minlength="8" maxlength="128" placeholder="Повторите новый пароль" required><div class="account-form-actions"><button class="account-primary" type="submit">Изменить пароль</button></div></form><p class="account-status" id="password-status" role="status"></p></section><section class="account-card"><header class="account-card-head"><h2>Дополнительные действия</h2><p>Управление аккаунтом и связанными данными.</p></header><div class="account-actions"><button class="account-action" type="button" id="export-account"><span>⇩</span><div><strong>Экспортировать данные</strong><small>Скачайте копию своих данных</small></div></button><button class="account-action danger" type="button" id="delete-account"><span>♲</span><div><strong>Удалить аккаунт</strong><small>Удалить аккаунт и все данные</small></div></button></div><p class="account-status" id="actions-status" role="status"></p></section></section>`;
    $("#account-profile-form").onsubmit = async (event) => { event.preventDefault(); const form = event.currentTarget, status = $("#profile-status"), button = form.querySelector("button"); button.disabled = true; status.textContent = "Сохраняем…"; try { const result = await request("/api/auth/profile", { method:"PATCH", body:JSON.stringify({ name:`${form.firstName.value} ${form.lastName.value}`.trim(), email:form.email.value }) }); Object.assign(user, result.user); status.textContent = "Изменения сохранены."; } catch(error) { status.textContent = error.message; } finally { button.disabled = false; } };
    $("#account-password-form").onsubmit = async (event) => { event.preventDefault(); const form = event.currentTarget, status = $("#password-status"), button = form.querySelector("button"); if (form.newPassword.value !== form.confirmPassword.value) return status.textContent = "Новые пароли не совпадают."; button.disabled = true; status.textContent = "Обновляем пароль…"; try { await request("/api/auth/password", { method:"PATCH", body:JSON.stringify({ currentPassword:form.currentPassword.value, newPassword:form.newPassword.value }) }); form.reset(); status.textContent = "Пароль изменён."; } catch(error) { status.textContent = error.message; } finally { button.disabled = false; } };
    $("#export-account").onclick = async () => { const status = $("#actions-status"); try { const projects = await request("/api/projects"), blob = new Blob([JSON.stringify({ user, projects, exportedAt:new Date().toISOString() }, null, 2)], { type:"application/json" }), link = document.createElement("a"); link.href = URL.createObjectURL(blob); link.download = "roomark-account-data.json"; link.click(); URL.revokeObjectURL(link.href); status.textContent = "Данные экспортированы."; } catch(error) { status.textContent = error.message; } };
    $("#delete-account").onclick = async () => { const status = $("#actions-status"); if (!confirm("Удалить аккаунт и все проекты без возможности восстановления?")) return; try { await request("/api/auth/account", { method:"DELETE" }); location.href = "./"; } catch(error) { status.textContent = error.message; } };
  }
  async function dashboard() { const projects = await request("/api/projects"), project = projects[0]; main.innerHTML = `<section class="studio-shell"><div class="studio-eyebrow"><p>Рабочее пространство / ${new Date().toLocaleDateString("ru", { month: "long", year: "numeric" })}</p><a class="button button-small" href="?view=new">Новый проект <span>＋</span></a></div><h1 class="studio-title">Добрый день, ${esc(user.name.split(" ")[0])}.<br><em>Продолжим работу?</em></h1>${project ? `<div class="dashboard-grid"><a class="project-feature" href="./project.html?id=${project.id}"><img src="./images/editorial-house.webp" alt=""><div class="project-feature-content"><p class="kicker light">Последний проект</p><h2>${esc(project.name)}</h2><div class="project-meta-line"><span>${project.area} м²</span><span>${project.rooms} комнат</span><span>${esc(project.status)}</span></div></div></a><aside class="dashboard-aside"><h3>Проект остаётся живым</h3><p>Редактируйте данные и модель на отдельной странице объекта.</p></aside></div>` : `<div class="empty-projects"><h2>Создайте первый проект</h2><a class="button" href="?view=new">Начать <span>→</span></a></div>`}</section>`; }
  async function list() { const projects = await request("/api/projects"); main.innerHTML = `<section class="studio-shell"><div class="studio-eyebrow"><p>Все объекты / ${projects.length}</p><a class="button button-small" href="?view=new">Создать проект <span>＋</span></a></div><h1 class="studio-title">Ваши <em>проекты</em></h1>${projects.length ? `<div class="projects-list">${projects.map(projectCard).join("")}</div>` : '<div class="empty-projects"><p>Здесь появятся ваши объекты.</p></div>'}</section>`; }
  function wizard() { let step = 1, data = { name: "", area: "", rooms: "", theme: "Тёплый", model: null }; const draw = () => { const titles = ["Расскажите об объекте", "Оформление и модель", "Проверьте детали"]; main.innerHTML = `<section class="studio-shell"><div class="studio-eyebrow"><p>Новый проект / шаг ${step} из 3</p><a href="?view=projects">Закрыть ×</a></div><div class="wizard"><ol class="wizard-steps">${["Объект", "Оформление", "Создание"].map((label, index) => `<li class="${index + 1 === step ? "active" : ""}"><span>${index + 1}</span>${label}</li>`).join("")}</ol><div class="wizard-main"><p class="kicker">Шаг 0${step}</p><h1>${titles[step - 1]}</h1><form id="wizard-form">${step === 1 ? `<div class="wizard-form"><label class="full">Название комплекса или объекта<input name="name" value="${esc(data.name)}" required minlength="2" placeholder="Дом у сосен"></label><label>Площадь, м²<input name="area" type="number" min="1" value="${esc(data.area)}" required></label><label>Количество помещений<input name="rooms" type="number" min="1" value="${esc(data.rooms)}" required></label></div>` : step === 2 ? `<div class="theme-choices">${["Тёплый", "Ночной", "Нейтральный"].map((theme) => `<button type="button" data-theme="${theme}" class="theme-choice ${data.theme === theme ? "active" : ""}"><span class="theme-swatch"></span><strong>${theme}</strong></button>`).join("")}</div><label class="file-drop"><input name="model" type="file" accept=".ifc,.glb"><span><strong>Выберите IFC или GLB</strong><p>Файл можно добавить сейчас или позже · до 200 МБ</p></span></label>` : `<div class="review-sheet"><dl><div><dt>Объект</dt><dd>${esc(data.name)}</dd></div><div><dt>Площадь</dt><dd>${esc(data.area)} м²</dd></div><div><dt>Комнаты</dt><dd>${esc(data.rooms)}</dd></div><div><dt>Оформление</dt><dd>${esc(data.theme)}</dd></div></dl></div>`}</form><p class="form-status" id="wizard-status"></p><div class="wizard-actions">${step > 1 ? '<button class="button-secondary" id="wizard-back">← Назад</button>' : "<span></span>"}<button class="button" id="wizard-next">${step === 3 ? "Создать проект" : "Продолжить"} <span>→</span></button></div></div></div></section>`; $$('[data-theme]').forEach((button) => button.onclick = () => { data.theme = button.dataset.theme; draw(); }); $("#wizard-back")?.addEventListener("click", () => { step--; draw(); }); $("#wizard-next").onclick = async (event) => { if (step === 1) { const form = $("#wizard-form"); if (!form.reportValidity()) return; data = { ...data, name: form.name.value, area: Number(form.area.value), rooms: Number(form.rooms.value) }; } if (step === 2) data.model = document.querySelector("#wizard-form").model.files[0] || data.model; if (step < 3) { step++; draw(); return; } event.currentTarget.disabled = true; try { const project = await request("/api/projects", { method: "POST", body: JSON.stringify(data) }); if (data.model) { const upload = new FormData(); upload.append("model", data.model); await request(`/api/projects/${project.id}/model`, { method: "POST", body: upload }); } location.href = `./project.html?id=${project.id}`; } catch (error) { $("#wizard-status").textContent = error.message; event.currentTarget.disabled = false; } }; }; draw(); }
  function billing() { main.innerHTML = `<section class="studio-shell"><div class="studio-eyebrow"><p>Аккаунт / Оплата</p></div><h1 class="studio-title">Тариф и <em>оплата</em></h1><div class="billing-card"><p class="kicker">Текущий тариф</p><h2>Пробный доступ</h2><p>Платёжная система пока не подключена. Здесь появятся тариф, история платежей и реквизиты.</p><button class="button" disabled>Выбрать тариф <span>→</span></button></div></section>`; }
  const result = (view === "projects" ? list : view === "new" ? wizard : view === "billing" ? billing : view === "dashboard" ? dashboard : profile)();
  if (result?.catch) result.catch((error) => { main.innerHTML = `<section class="studio-shell"><p class="form-status">${esc(error.message)}</p></section>`; });
})();
