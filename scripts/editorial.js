(()=>{const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];const KEY='houseReviewerProjects';const seed=[{id:'severny',name:'ЖК Северный',area:95,rooms:3,theme:'Тёплый',status:'Опубликован',updated:'Сегодня',image:'./images/editorial-house.webp'}];const projects=()=>{try{return JSON.parse(localStorage.getItem(KEY))||seed}catch{return seed}};const save=v=>localStorage.setItem(KEY,JSON.stringify(v));
const observer='IntersectionObserver'in window?new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.12}):null;$$('.reveal').forEach(el=>observer?observer.observe(el):el.classList.add('visible'));
const authForm=$('#auth-form');if(authForm){let mode=new URLSearchParams(location.search).get('mode')==='register'?'register':'login';const render=()=>{$$('[data-auth-mode]').forEach(b=>b.classList.toggle('active',b.dataset.authMode===mode));$('#name-field').hidden=mode==='login';$('#auth-title').textContent=mode==='login'?'Войти в кабинет':'Создать аккаунт';$('#auth-kicker').textContent=mode==='login'?'С возвращением':'Начало нового проекта';$('#auth-submit-text').textContent=mode==='login'?'Войти':'Зарегистрироваться';authForm.password.autocomplete=mode==='login'?'current-password':'new-password'};$$('[data-auth-mode]').forEach(b=>b.onclick=()=>{mode=b.dataset.authMode;history.replaceState({},'',`?mode=${mode}`);render()});authForm.onsubmit=e=>{e.preventDefault();const data=new FormData(authForm);localStorage.setItem('houseReviewerUser',JSON.stringify({name:data.get('name')||'Анна',email:data.get('email')}));location.href='./studio.html'};render()}
const main=$('#studio-main');if(!main)return;const user=(()=>{try{return JSON.parse(localStorage.getItem('houseReviewerUser'))}catch{return null}})()||{name:'Анна'};$$('.profile-name').forEach(e=>e.textContent=user.name.split(' ')[0]);$('#logout-button').onclick=()=>{localStorage.removeItem('houseReviewerUser');location.href='./'};const view=new URLSearchParams(location.search).get('view')||'dashboard';$$('[data-studio-nav]').forEach(a=>a.classList.toggle('active',a.dataset.studioNav===view));
const projectRows=list=>list.length?`<div class="projects-list">${list.map(p=>`<a class="project-row" href="./project.html?id=${encodeURIComponent(p.id)}"><img src="${p.image||'./images/editorial-house.webp'}" alt=""><div><h3>${p.name}</h3><p>${p.area} м² · ${p.rooms} комнаты</p></div><small>${p.updated}</small><span class="status-dot">${p.status}</span><span class="row-arrow">→</span></a>`).join('')}</div>`:`<div class="empty-projects"><h2>Здесь появятся ваши объекты</h2><p>Создайте первый проект — это займёт несколько минут.</p></div>`;
function dashboard(){const list=projects(),p=list[0]||seed[0];main.innerHTML=`<section class="studio-shell"><div class="studio-eyebrow"><p>Рабочее пространство / ${new Date().toLocaleDateString('ru',{month:'long',year:'numeric'})}</p><a class="button button-small" href="?view=new">Новый проект <span>＋</span></a></div><h1 class="studio-title">Добрый день, ${user.name.split(' ')[0]}.<br><em>Продолжим работу?</em></h1><div class="dashboard-grid"><a class="project-feature" href="./project.html?id=${encodeURIComponent(p.id)}"><img src="${p.image}" alt="${p.name}"><div class="project-feature-content"><p class="kicker light">Последний проект</p><h2>${p.name}</h2><div class="project-meta-line"><span>${p.area} м²</span><span>${p.rooms} комнаты</span><span>${p.status}</span></div></div></a><aside class="dashboard-aside"><div><p class="kicker">Коротко о главном</p><h3>Проекты остаются живыми</h3><p>Добавляйте материалы и замечания по мере развития объекта — публичная ссылка обновится сама.</p></div><div><div class="metric"><strong>${list.length}</strong><span>проектов в работе</span></div><div class="metric"><strong>1</strong><span>публичная ссылка</span></div></div></aside></div></section>`}
function list(){const ps=projects();main.innerHTML=`<section class="studio-shell"><div class="studio-eyebrow"><p>Все объекты / ${ps.length}</p><a class="button button-small" href="?view=new">Создать проект <span>＋</span></a></div><h1 class="studio-title">Ваши <em>проекты</em></h1>${projectRows(ps)}</section>`}
function wizard(){let step=1,data={name:'',area:'',rooms:'',theme:'Тёплый',file:''};const draw=()=>{const texts=[['Расскажите об объекте','Начнём с основного: название, площадь и количество помещений.'],['Выберите настроение','Оформление можно изменить позже — содержимое проекта останется прежним.'],['Добавьте модель','Загрузите IFC или GLB сейчас или пропустите этот шаг.'],['Проверьте детали','Один универсальный шаблон соберёт сайт из этих данных.']];main.innerHTML=`<section class="studio-shell"><div class="studio-eyebrow"><p>Новый проект / шаг ${step} из 4</p><a href="?view=projects">Закрыть ×</a></div><div class="wizard"><ol class="wizard-steps">${['Объект','Оформление','3D-модель','Публикация'].map((x,i)=>`<li class="${i+1===step?'active':''}"><span>${i+1}</span>${x}</li>`).join('')}</ol><div class="wizard-main"><p class="kicker">Шаг 0${step}</p><h1>${texts[step-1][0]}</h1><p class="wizard-lede">${texts[step-1][1]}</p><form id="wizard-form">${stepMarkup(step,data)}</form><div class="wizard-actions">${step>1?'<button class="button-secondary" id="wizard-back">← Назад</button>':'<span></span>'}<button class="button" id="wizard-next">${step===4?'Создать проект':'Продолжить'} <span>→</span></button></div></div></div></section>`;$$('.theme-choice').forEach(b=>b.onclick=()=>{data.theme=b.dataset.theme;draw()});$('#wizard-back')?.addEventListener('click',()=>{step--;draw()});$('#wizard-next').onclick=()=>{const form=$('#wizard-form');if(step===1){if(!form.reportValidity())return;data.name=form.name.value;data.area=form.area.value;data.rooms=form.rooms.value}if(step===3)data.file=form.model.files[0]?.name||'';if(step<4){step++;draw()}else{const ps=projects();const id=Date.now().toString(36);ps.unshift({id,name:data.name,area:Number(data.area),rooms:Number(data.rooms),theme:data.theme,status:'Черновик',updated:'Только что',image:'./images/editorial-house.webp',model:data.file});save(ps);location.href=`./project.html?id=${encodeURIComponent(id)}`}}};draw()}
function stepMarkup(step,d){if(step===1)return`<div class="wizard-form"><label class="full">Название ЖК или объекта<input name="name" value="${d.name}" placeholder="Например, Дом у сосен" required></label><label>Площадь, м²<input name="area" type="number" min="1" value="${d.area}" placeholder="128" required></label><label>Количество комнат<input name="rooms" type="number" min="1" value="${d.rooms}" placeholder="5" required></label></div>`;if(step===2)return`<div class="theme-choices">${['Тёплый','Ночной','Нейтральный'].map(x=>`<button type="button" data-theme="${x}" class="theme-choice ${d.theme===x?'active':''}"><span class="theme-swatch"></span><strong>${x}</strong></button>`).join('')}</div>`;if(step===3)return`<div class="wizard-form"><label class="file-drop"><input name="model" type="file" accept=".ifc,.glb"><span><strong>Перетащите IFC или GLB</strong><p>или нажмите, чтобы выбрать файл · до 200 МБ</p></span></label></div>`;return`<div class="review-sheet"><dl><div><dt>Объект</dt><dd>${d.name}</dd></div><div><dt>Площадь</dt><dd>${d.area} м²</dd></div><div><dt>Комнаты</dt><dd>${d.rooms}</dd></div><div><dt>Оформление</dt><dd>${d.theme}</dd></div><div><dt>Модель</dt><dd>${d.file||'Добавить позже'}</dd></div><div><dt>Адрес</dt><dd>Будет создан автоматически</dd></div></dl></div>`}
if(view==='projects')list();else if(view==='new')wizard();else dashboard()})();
